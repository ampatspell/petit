<div class="group color">
  <div class="label">{{@label}}</div>
  <Block::Node::Palette::Color::Rect class="color" @size="wide" @color={{@model}}/>
  {{#each this.props as |key|}}
    <div class="prop">
      <Input::Slider
        class="slider"
        @value={{get @model key}}
        @disabled={{not @model.editable}}
        @min={{0}}
        @max={{255}}
        @onInput={{fn this.onChange key}}
      />
      <Input::Text
        class={{class "input" "small"}}
        @disabled={{not @model.editable}}
        @value={{get @model key}}
        @type="int"
        @onChange={{fn this.onChange key}}
      />
    </div>
  {{/each}}
  <Input::Text @disabled={{not @model.editable}} @value={{this.hex}} @onChange={{this.onHex}}/>
</div>