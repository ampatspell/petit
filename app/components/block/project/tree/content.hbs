{{#each @array as |node|}}

  <@tree.item
    class={{node.type}}
    @selected={{node.selected}}
    @locked={{node.locked}}
    @parentLocked={{node.parentLocked}}
    @hidden={{node.hidden}}
    @parentHidden={{node.parentHidden}}
    @expanded={{node.expanded}}
    @expandable={{node.tree.expandable}}
    @lockable={{node.tree.lockable}}
    @hideable={{node.tree.hideable}}
    @warning={{node.hasWarnings}}
    @onSelect={{fn @onSelect node}}
    @onUpdate={{fn @onUpdate node}}
  >
    <:content>
      <div class="row">{{node.typeName}}</div>
      <div class="row strong">{{or node.identifier 'No identifier'}}</div>
    </:content>
    <:items>
      <Block::Project::Tree::Content
        @array={{node.children}}
        @tree={{@tree}}
        @onSelect={{@onSelect}}
        @onUpdate={{@onUpdate}}
      />
    </:items>
  </@tree.item>

{{/each}}