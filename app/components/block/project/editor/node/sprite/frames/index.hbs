{{#if this.frame}}

  <Konva::Stage @size={{this.size}}
    as |parent|>

    <Konva::Node::Layer @parent={{parent}} as |parent|>

      <Block::Project::Editor::Node::Sprite::Frames::Frame::Editor
        @parent={{parent}}
        @size={{this.size}}
        @frame={{this.frame}}
        @editing={{this.editing}}
        @color={{this.color}}
        @palette={{this.palette}}
        @onStartEditing={{this.startEditing}}
      />

    </Konva::Node::Layer>
  </Konva::Stage>

  {{#if this.editing}}
    <Application::InTools>
      Tools for frame editor
    </Application::InTools>
  {{/if}}

{{/if}}
